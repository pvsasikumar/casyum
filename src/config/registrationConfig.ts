/**
 * Central configuration for public registration.
 *
 * Public registration for CASYUM is handled entirely through Google Forms —
 * there is no in-app participant registration or authentication. Every public
 * "Register" action opens this form in a new browser tab.
 *
 * Replace this URL with the live Google Form link when it is ready. This is
 * the single source of truth, so the form link only ever changes here.
 */
export const REGISTRATION_FORM_URL = 'https://forms.gle/oRqjXTCSbe9HzqLZ8';